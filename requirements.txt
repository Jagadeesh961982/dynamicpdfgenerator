# ── Core pipeline (all required) ──────────────────────────────────
# No extra dependencies for base flow — uses only Python stdlib

# ── Better console output (highly recommended) ────────────────────
rich>=13.0.0

# ── CSV input support ─────────────────────────────────────────────
pandas>=2.0.0

# ── PDF input support (to use .pdf files as input) ────────────────
pdfplumber>=0.10.0
# or: PyPDF2>=3.0.0

# ── PDF export (install ONE of these) ─────────────────────────────
# Option A — Playwright (recommended, best quality)
playwright>=1.40.0
#   After pip install, also run:
#   playwright install chromium

# Option B — pdfkit (needs wkhtmltopdf binary installed separately)
# pdfkit>=1.0.0

# ── HTTP API (optional) — run from fastapi-app/: uvicorn app.main:app ──
fastapi>=0.110.0                     # ASGI routes
uvicorn[standard]>=0.27.0            # production server
python-multipart>=0.0.9             # file uploads
pydantic[email]>=2.0.0              # EmailStr
pydantic-settings>=2.0.0
email-validator>=2.0.0
sqlalchemy>=2.0.0
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
cryptography>=42.0.0
pytest>=8.0.0                        # fastapi-app/app/tests
httpx>=0.27.0                         # TestClient
