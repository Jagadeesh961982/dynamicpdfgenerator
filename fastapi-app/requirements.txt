# Install from fastapi-app/:
#   pip install -r requirements.txt
#   playwright install chromium   (needed for PDF export)

# ── Console output ─────────────────────────────────────────────────
rich>=13.0.0

# ── Input formats ──────────────────────────────────────────────────
pandas>=2.0.0          # CSV input
pdfplumber>=0.10.0     # PDF input

# ── Export ─────────────────────────────────────────────────────────
playwright>=1.40.0     # PDF export (run: playwright install chromium)
python-pptx>=0.6.21   # PPTX export

# ── Browser / Web Research Agent (BROWSER_ENABLED=true) ───────────
duckduckgo-search>=6.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── Environment ────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── FastAPI stack ──────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic[email]>=2.0.0
pydantic-settings>=2.0.0
email-validator>=2.0.0
sqlalchemy>=2.0.0
bcrypt>=4.0.1
python-jose[cryptography]>=3.3.0
cryptography>=42.0.0

# ── Testing ────────────────────────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0
