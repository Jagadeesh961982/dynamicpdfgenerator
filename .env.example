# Web/API secrets and tunables live next to the FastAPI app:
#   fastapi-app/.env
#
# See fastapi-app/.env.example and run the API from fastapi-app/:
#   set PYTHONPATH=%CD%\..
#   cd fastapi-app
#   uvicorn app.main:app --reload
